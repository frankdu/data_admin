# Encryption key for storing database connection strings securely
# Generate a secure key using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# If not provided, a random key will be generated on each server restart (sessions will be lost on restart)
ENCRYPTION_KEY=your-64-character-hex-string-here
